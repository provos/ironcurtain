#!/usr/bin/env bash
# Pre-commit hook: runs format check and lint on staged files only.
# Install: cp .hooks/pre-commit .git/hooks/pre-commit && chmod +x .git/hooks/pre-commit
# Or:      npm run setup-hooks

set -euo pipefail

npm run pre-commit

echo "Pre-commit checks passed."
